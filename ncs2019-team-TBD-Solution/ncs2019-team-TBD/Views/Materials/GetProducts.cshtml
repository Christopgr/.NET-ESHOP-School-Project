@*
    For more information on enabling MVC for empty projects, visit http://go.microsoft.com/fwlink/?LinkID=397860
*@


@model ncs2019_team_TBD.Controllers.MaterialsController.MatProd



@{
    ViewData["Title"] = "GetProducts";
}

<h2>Index</h2>

<p>
    Products of the category
</p>


<div class="row">

    @*prepei na prosexoume pws onomazoume tis eikones, giati me tin sira tou i pernane sto view, epidi etsi tis kaloume*@
    @{
        int i = 1;
        @foreach (var item in Model.ProductList)
        {
            <div class="col-sm-6 col-md-4">
                <div class="thumbnail">
                    <img src="@Url.Content(string.Format("~/images/"+i+".jpg", i++))" alt=" @Html.DisplayFor(modelItem => item.Id) " width="242" height="200">
                    <div class="caption">
                        <h3> @Html.DisplayFor(modelItem => item.Name) </h3>
                        <p> @Html.DisplayFor(modelItem => item.Description) </p>
                        <p>
                            <a asp-action="Add" asp-route-productId="@item.Id" asp-route-quantity="1" asp-route-materialId="@Model.Mat.Id" class="btn btn-primary" role="button">Add to Cart</a>
                            @*stin akrivos panw grami  ftiaxnes to path gia to "Add to Cart"*@
                            <a asp-action="Details" class="btn btn-primary" role="button" asp-route-id="@item.Id">Details</a>

                            <u><strong> @Html.DisplayFor(modelItem => item.Price) </strong></u>
                            <span class="glyphicon glyphicon-eur" aria-hidden="true"></span>
                        </p>

                    </div>
                </div>
            </div>
        }
    }
</div>